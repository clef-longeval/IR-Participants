System Name:
Description (short description of the system):
Ranking Methods (Which ranking approaches does the system use?):
System Combination / Fusion (Does the system combine different retrieval models? If so, which? If so, how are they combined?):
Data Used (Which data did you use to train or fine-tune the system?):
Multi-stage Retrieval (Does the system use reranking? If so, what rerankers are used?):
Translations (Does the system use French documents or the translations? If you use translations, which ones?):
Pre-processing and Indexing (What pre-processing and indexing does the system use?):
The Cost (How efficient is the system in the pre-processing and interference steps?):
----
Did you use any statistical ranking model? (yes/no):
Did you use any deep neural network model? (yes/no):
Did you use sparse neural model? (yes/no):
Did you use dense neural model? (yes/no):
Did you use French or English documents (French/English/both):
Did you use any manual intervention on the translations? (yes/no):
Did you use any manual intervation on the results? (yes/no):
